# Dependencies — auto-generated by `npm install`, never committed.
node_modules/

# OS / editor cruft
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/

# Local env secrets — the real keys live in your host's dashboard, not the repo.
.env
.env.local

# NOTE: assets/media/ (your vendored images) and site/ (the build output) ARE
# committed on purpose — do not add them here.
